host: localhost
listen: ':8443'
root: ./public

log:
  output: stderr

ssl:
  certFile: ./ssl/localhost.crt
  keyFile: ./ssl/localhost.key

handlers:

  static:
    type: fs
    indexNames: ['index.html']

routes:

  - path: /
    handler: static
